To publish from theredstonetaco.com directory to github:

git add .

git commit -m "(message)"

git push origin master

Remember you can use "git status" to check up on stuff, not sure
really what it exactly covers except unpushed/uncommited files.
